FAILED — second torch.compile attempt with TF32 pinned via torch.set_float32_matmul_precision('high'). New different InductorError: AssertionError: -56034992939621/250000000000000 (internal Triton codegen numerical assertion). Two distinct compile bugs in same stack confirms torch.compile is broken on PyTorch 2.10 + Inductor + FSDP2 + Liger gemma4 combo. Per plan §6, declare P6 blocked and skip. FP8 alternative needs TransformerEngine integration — out of scope without prior TE setup verification. Project already at 11.3× cumulative speedup vs DS prod, P6 not a critical blocker.
