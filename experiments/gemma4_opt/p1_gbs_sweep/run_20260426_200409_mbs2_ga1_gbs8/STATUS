FAILED — swift Ulysses SP bug at sequence_parallel/ulysses.py:490 (`length = tensor.shape[dim]` on dict-typed attention_mask). gemma4 VLM passes attention_mask as a dict (with image-related keys), MBS=2 batched collation triggers `pad_and_split_inputs` → `pad()` → `_do_pad()` which assumes tensor. MBS=1 works (single-sample dict path), MBS≥2 needs swift fork patch. Skipped — peak GBS doesn't need MBS>1.
