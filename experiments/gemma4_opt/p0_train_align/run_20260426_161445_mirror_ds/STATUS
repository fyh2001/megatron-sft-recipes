FAILED exit_code=1 — but α SUCCEEDED. forward + backward + opt-state lazy-alloc + clip_grad_norm ALL passed (mixed backend cpu:gloo,cuda:nccl works perfectly). New blocker: bnb PagedAdamW32bit.prefetch_state at bitsandbytes/optim/optimizer.py:379 calls F.prefetch_tensor → CUDA cprefetch(ptr, size, deviceid) but deviceid=None because FSDP2 CPUOffloadPolicy puts params on CPU (DTensor offloaded shards have no device.index). bnb expects CUDA params for paged optimizer. Fixed in run_10 by switching to --optim adamw_torch (device-agnostic, runs CPU math on CPU params, numerically identical to paged_adamw_32bit since both use fp32 master + m + v).
