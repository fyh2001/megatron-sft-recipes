FAILED exit_code=1 — RuntimeError 'No backend type associated with device type cpu' at clip_grad_norm. HUGE PROGRESS: forward + backward + optimizer state lazy-alloc all SUCCEEDED. Only clip_grad_norm fails because PyTorch FSDP2 CPUOffloadPolicy puts grads on CPU, then DTensor.linalg.vector_norm calls all_reduce via NCCL which can't handle CPU tensors. accelerate's clip path doesn't route this through DS-style optimizer.clip_grad_norm. Fixed in run_09 by adding sitecustomize patch (3/3) that intercepts init_process_group to use 'cpu:gloo,cuda:nccl' mixed backend (verified by dist_backend_smoke.py).
