FAILED exit_code=1 — CUDA OOM at step 0/40. swift Ulysses SP global-layer (head_dim=512) fallback to SDPA math backend allocated ~4.3 GB attn matrix on top of ~52 GB FSDP2 idle resident (AdamW master+m+v fp32 sharded). truncation=right exposed first sample at full 16384 tokens vs baseline's delete (resampled to 919 tok). Fixed in run_03 by adding 'offload' to fsdp string → CPUOffloadPolicy, mirroring DS prod baseline's offload(opt+param) CPU.
