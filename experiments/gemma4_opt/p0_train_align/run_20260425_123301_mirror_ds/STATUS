FAILED exit_code=1 — CUDA OOM in BACKWARD (forward succeeded! offload partially worked). accelerate fsdp 'offload' only offloads params + grads (~13 GB freed), NOT AdamW optimizer state (master+m+v fp32 = ~39 GB still GPU-resident). Backward needs 4.27 GB SDPA matrix again, peak 78.46 GB / 80 GB. Fixed in run_04 by adding --optim paged_adamw_32bit (bnb paged opt state to CPU pinned mem, numerically equivalent to default AdamW).
