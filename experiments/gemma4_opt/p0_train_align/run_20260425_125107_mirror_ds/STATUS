FAILED exit_code=1 — RuntimeError: Invalid backend. mem_efficient SDPA was correctly enabled but transformers/integrations/sdpa_attention.py:39 use_gqa_in_sdpa returns True when attention_mask is None and torch>=2.5, causing the wrapper to pass enable_gqa=True kwarg to torch.nn.functional.scaled_dot_product_attention. mem_efficient backend does NOT support enable_gqa=True (verified by sdp_gqa_smoke.py: 'Invalid backend' with mem_eff only; uses math fallback if math also enabled, which gives 20.5 GB OOM). Fixed in run_06 by extending sitecustomize to monkey-patch use_gqa_in_sdpa → False, forcing repeat_kv pre-expansion (functionally equivalent, mem_efficient happily handles expanded KV at 384 MB).
