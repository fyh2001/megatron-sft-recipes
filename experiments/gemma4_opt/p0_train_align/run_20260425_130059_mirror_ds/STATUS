FAILED exit_code=1 — swift's activation_cpu_offload callback bug. AssertionError at swift/callbacks/activation_cpu_offload.py:327 'assert not isinstance(tensor, tuple)' on first backward — PyTorch's checkpoint mechanism on FSDP2 stores saved tensors as a tuple but swift's tensor_pop assumes non-tuple. Removing activation_cpu_offload, keeping fsdp 'offload' + mem_eff + paged_adamw + GQA patch in run_08.
